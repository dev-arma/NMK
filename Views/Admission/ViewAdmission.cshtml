@model NMK.Models.Admission

<h2 class="text-center my-4">Detalji Nalaza</h2>

<div class="row">
    <div class="col-md-6">
        <h4>Podaci o pacijentu</h4>
        <p><strong>Ime i prezime:</strong> @Model.Patient.NameAndSurname</p>
        <p><strong>Datum prijema:</strong> @Model.DateAdmitted.ToString("dd/MM/yyyy")</p>
    </div>
    <div class="col-md-6">
        <h4>Podaci o liječniku</h4>
        <p><strong>Ime i prezime:</strong> @Model.Doctor.Surname, @Model.Doctor.Name</p>
        <p><strong>Titula:</strong> @Model.Doctor.Title</p>
    </div>
</div>

<h4 class="my-4">Medicinski nalaz</h4>
<p><strong>Datum nalaza:</strong> @Model.DateAdmitted.ToString("dd/MM/yyyy")</p>
<p><strong>Tekst nalaza:</strong> @Model.ReportText</p>



<div class="mt-3">
    <a href="@Url.Action("Index")" class="btn btn-secondary">Povratak na prijeme</a>
</div>
